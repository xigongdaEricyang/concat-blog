'use strict';

/**
 * sitemap-route-override service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sitemap-route-override.sitemap-route-override');
